# 📮 留言功能配置指南

## 🔍 问题诊断

您的朋友无法成功留言，原因是 **Resend API Key 未配置**。

留言功能使用 Resend 服务发送邮件，需要配置 API Key 才能正常工作。

## ✅ 解决方案

### 步骤 1：注册 Resend 账号

1. 访问 [Resend 官网](https://resend.com)
2. 点击 "Sign Up" 注册账号（可以使用 GitHub 账号快速登录）
3. 完成邮箱验证

### 步骤 2：获取 API Key

1. 登录 Resend 控制台
2. 进入 "API Keys" 页面
3. 点击 "Create API Key"
4. 输入名称（如：`蛋蛋小屋留言功能`）
5. 选择权限：`Sending access`
6. 点击 "Create" 创建
7. **重要**：复制生成的 API Key（只显示一次，请妥善保存）

### 步骤 3：验证发件域名（可选但推荐）

**使用 Resend 提供的测试域名**（免费）：
- Resend 提供 `onboarding@resend.dev` 作为测试发件地址
- 每天可发送 100 封邮件
- 适合个人网站使用

**使用自己的域名**（推荐）：
1. 在 Resend 控制台点击 "Domains"
2. 点击 "Add Domain"
3. 输入您的域名（如：`example.com`）
4. 按照提示添加 DNS 记录：
   - SPF 记录
   - DKIM 记录
   - DMARC 记录（可选）
5. 等待 DNS 验证通过（通常需要几分钟到几小时）

### 步骤 4：配置 Supabase 环境变量

1. 访问 [Supabase 控制台](https://supabase.com/dashboard)
2. 选择您的项目
3. 进入 "Project Settings" → "Edge Functions"
4. 找到 "Secrets" 部分
5. 添加新的环境变量：
   - **Name**: `RESEND_API_KEY`
   - **Value**: 粘贴您在步骤 2 中获取的 API Key
6. 点击 "Save" 保存

**或者使用命令行配置**：

```bash
# 安装 Supabase CLI（如果还没安装）
npm install -g supabase

# 登录 Supabase
supabase login

# 链接到您的项目
supabase link --project-ref YOUR_PROJECT_REF

# 设置环境变量
supabase secrets set RESEND_API_KEY=your_resend_api_key_here
```

### 步骤 5：更新 Edge Function 配置（如果使用自己的域名）

如果您使用自己的域名，需要修改 Edge Function 中的发件地址：

编辑文件：`supabase/functions/send-contact-email/index.ts`

找到第 81-82 行：

```typescript
from: 'Egg Cabin <onboarding@resend.dev>',
reply_to: email,
```

修改为：

```typescript
from: 'Egg Cabin <noreply@yourdomain.com>',  // 改为您的域名
reply_to: email,
```

然后重新部署 Edge Function。

### 步骤 6：重新部署 Edge Function

如果您修改了 Edge Function 代码，需要重新部署：

```bash
# 使用 Supabase CLI 部署
supabase functions deploy send-contact-email
```

或者在 Supabase 控制台中重新部署。

### 步骤 7：测试留言功能

1. 访问您的网站
2. 进入"留言簿"页面
3. 填写表单：
   - 名字：测试用户
   - 邮箱：your-email@example.com
   - 留言：这是一条测试留言
4. 点击"寄出明信片"
5. 检查是否收到成功提示
6. 检查您的邮箱（1660296253@qq.com）是否收到留言

## 🎯 配置检查清单

- [ ] 已注册 Resend 账号
- [ ] 已获取 Resend API Key
- [ ] 已在 Supabase 中配置 RESEND_API_KEY 环境变量
- [ ] （可选）已验证自己的域名
- [ ] （可选）已更新发件地址
- [ ] 已重新部署 Edge Function
- [ ] 已测试留言功能

## 🔧 故障排查

### 问题 1：提示"邮件服务配置错误"

**原因**：RESEND_API_KEY 未配置或配置错误

**解决方案**：
1. 检查 Supabase 环境变量是否正确配置
2. 确认 API Key 没有多余的空格
3. 重新部署 Edge Function

### 问题 2：提示"邮件发送失败"

**原因**：Resend API 调用失败

**可能的原因**：
1. API Key 无效或已过期
2. 发件地址未验证（如果使用自己的域名）
3. 超出发送限额（免费版每天 100 封）

**解决方案**：
1. 检查 Resend 控制台的 API Key 状态
2. 验证域名或使用测试域名
3. 检查发送限额

### 问题 3：没有收到邮件

**原因**：邮件被拦截或进入垃圾箱

**解决方案**：
1. 检查垃圾邮件文件夹
2. 将发件地址添加到白名单
3. 如果使用 QQ 邮箱，检查是否开启了反垃圾设置

### 问题 4：前端显示"发送失败，请检查网络连接"

**原因**：Edge Function 调用失败

**解决方案**：
1. 打开浏览器开发者工具（F12）
2. 查看 Console 标签的错误信息
3. 查看 Network 标签的请求详情
4. 检查 Supabase 项目状态

## 📊 Resend 免费版限额

- **每天发送**：100 封邮件
- **每月发送**：3,000 封邮件
- **测试域名**：可用
- **自定义域名**：1 个
- **API 调用**：无限制

对于个人网站来说，免费版完全够用！

## 🎨 自定义邮件模板

如果您想自定义邮件样式，可以编辑 Edge Function 中的邮件模板：

文件：`supabase/functions/send-contact-email/index.ts`

找到第 84-209 行的 HTML 模板，修改样式和内容。

## 📚 相关文档

- [Resend 官方文档](https://resend.com/docs)
- [Supabase Edge Functions 文档](https://supabase.com/docs/guides/functions)
- [Supabase Secrets 管理](https://supabase.com/docs/guides/functions/secrets)

## ✨ 配置完成后

配置完成后，您的留言功能将：

1. ✅ 接收访客留言
2. ✅ 自动发送到您的邮箱（1660296253@qq.com）
3. ✅ 显示成功动画和提示
4. ✅ 包含访客的联系方式，方便回复
5. ✅ 使用精美的邮件模板

## 🎉 快速配置（5 分钟完成）

如果您只想快速启用功能：

1. 注册 Resend（1 分钟）
2. 获取 API Key（1 分钟）
3. 在 Supabase 配置环境变量（2 分钟）
4. 测试功能（1 分钟）

**就这么简单！** 🚀

---

**文档创建时间**：2025-12-02  
**适用版本**：蛋蛋小屋 v1.0

如有问题，请查看浏览器控制台的错误信息，或联系技术支持。
