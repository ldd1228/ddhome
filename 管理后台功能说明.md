# 管理后台功能说明

## 功能概述

蛋蛋小屋现在拥有完整的管理后台系统，管理员可以通过密码登录后台，查看网站访客记录和留言信息。

## 访问方式

### 1. 登录页面
访问地址：`/admin/login`

例如：
- 本地开发：`http://localhost:5173/admin/login`
- 生产环境：`https://your-domain.com/admin/login`

### 2. 默认密码
- 默认密码：`admin123`
- 可通过环境变量 `VITE_ADMIN_PASSWORD` 修改

## 功能特性

### 🔐 安全登录
- 密码验证保护
- 登录状态保持（24小时）
- 自动过期检测
- 退出登录功能

### 👥 访客管理
查看所有访问网站的访客信息：
- 访问时间
- 访问页面
- IP 地址
- 设备信息（浏览器、操作系统）
- 屏幕分辨率
- 语言设置
- 来源页面

### 💬 留言管理
查看所有用户提交的留言：
- 留言内容
- 提交时间
- 联系方式（邮箱）
- 留言类型（文字/画板）
- 画板作品预览

## 使用流程

### 第一步：访问登录页面
1. 在浏览器中打开 `/admin/login`
2. 看到登录界面

### 第二步：输入密码
1. 在密码输入框中输入管理员密码
2. 默认密码：`admin123`
3. 点击"登录"按钮

### 第三步：查看管理后台
登录成功后，自动跳转到管理后台，可以看到：
- 顶部导航栏：显示网站名称、返回首页、退出登录按钮
- 两个标签页：
  - **访客管理**：查看所有访客记录
  - **留言管理**：查看所有留言信息

### 第四步：查看访客记录
1. 点击"访客管理"标签
2. 查看访客列表，包含：
   - 访问时间
   - 访问页面
   - 访客设备信息
   - 访客位置信息
3. 可以按时间、页面等条件筛选

### 第五步：查看留言信息
1. 点击"留言管理"标签
2. 查看留言列表，包含：
   - 留言内容
   - 提交时间
   - 联系邮箱
   - 留言类型
3. 如果是画板留言，可以预览画作

### 第六步：退出登录
1. 点击顶部"退出登录"按钮
2. 自动跳转回登录页面
3. 登录状态已清除

## 修改管理员密码

### 方法一：修改 .env 文件
1. 打开项目根目录的 `.env` 文件
2. 找到或添加以下行：
   ```
   VITE_ADMIN_PASSWORD=your_new_password
   ```
3. 将 `your_new_password` 替换为你的新密码
4. 保存文件
5. 重启开发服务器

### 方法二：设置环境变量
在部署平台（如 Vercel、Netlify）设置环境变量：
- 变量名：`VITE_ADMIN_PASSWORD`
- 变量值：你的新密码

## 安全建议

### 1. 修改默认密码
- ⚠️ 强烈建议修改默认密码 `admin123`
- 使用强密码（包含大小写字母、数字、特殊字符）
- 定期更换密码

### 2. 不要分享密码
- 不要将密码告诉他人
- 不要在公共场合输入密码
- 不要将密码保存在浏览器中

### 3. 及时退出登录
- 使用完毕后及时退出登录
- 不要在公共电脑上保持登录状态
- 登录状态会在 24 小时后自动过期

### 4. 保护环境变量
- 不要将 `.env` 文件提交到 Git 仓库
- 不要在代码中硬编码密码
- 使用环境变量管理敏感信息

## 技术实现

### 登录验证
- 使用 sessionStorage 保存登录状态
- 登录时间戳记录，24 小时自动过期
- 密码存储在环境变量中

### 路由保护
- 访问管理后台前检查登录状态
- 未登录自动跳转到登录页面
- 登录过期自动跳转到登录页面

### 数据查询
- 使用 Supabase 数据库查询访客和留言数据
- 实时数据更新
- 支持分页和筛选

## 常见问题

### Q1: 忘记密码怎么办？
A: 查看 `.env` 文件中的 `VITE_ADMIN_PASSWORD` 变量，或者重新设置该变量。

### Q2: 登录后无法访问管理后台？
A: 检查浏览器控制台是否有错误信息，确保 Supabase 配置正确。

### Q3: 登录状态为什么会丢失？
A: 登录状态保存在 sessionStorage 中，关闭浏览器标签页会清除。另外，登录状态会在 24 小时后自动过期。

### Q4: 如何在生产环境设置密码？
A: 在部署平台（如 Vercel）的环境变量设置中添加 `VITE_ADMIN_PASSWORD` 变量。

### Q5: 可以添加多个管理员吗？
A: 当前版本只支持一个管理员密码。如需多管理员功能，需要扩展用户管理系统。

## 未来计划

- [ ] 支持多管理员账号
- [ ] 添加访客统计图表
- [ ] 留言回复功能
- [ ] 数据导出功能
- [ ] 访客地理位置可视化
- [ ] 实时访客在线监控

## 相关文件

- 登录页面：`src/pages/AdminLogin.tsx`
- 管理后台页面：`src/pages/AdminDashboardPage.tsx`
- 管理后台组件：`src/components/admin/AdminDashboard.tsx`
- 访客管理组件：`src/components/admin/VisitorManagement.tsx`
- 留言管理组件：`src/components/admin/MessageManagement.tsx`
- 路由配置：`src/routes.tsx`
- 环境变量：`.env`

## 支持

如有问题或建议，请联系管理员。
